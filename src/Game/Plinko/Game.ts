import { Plinko } from ".";

export class GameEngine {
  constructor(main: Plinko) {}
  private canvas: HTMLCanvasElement | null = null;
  // private app

  create = (canvas: HTMLCanvasElement | null) => {};
}
